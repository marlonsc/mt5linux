# =============================================================================
# MT5Linux Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values.
# All variables use MT5_ prefix (handled by Pydantic BaseSettings in config.py).
#
# Configuration Source: mt5linux/config.py (MT5Settings) - Single Source of Truth
# See MT5Settings class for all available parameters and their defaults.
# =============================================================================

# === MT5 Credentials (REQUIRED) ===
# Your MetaTrader5 demo account credentials
MT5_LOGIN=your_demo_login
MT5_PASSWORD=your_password
MT5_SERVER=MetaQuotes-Demo

# === Network (Optional - defaults shown) ===
# MT5_HOST=localhost

# === Docker Ports (Optional) ===
# For pytest tests (isolated from production):
#   MT5_GRPC_PORT=28812 (test default)
#   MT5_VNC_PORT=23000 (test default)
#   MT5_HEALTH_PORT=28002 (test default)
#
# For production/direct Docker access:
#   MT5_GRPC_PORT=38812 (docker default)
#   MT5_VNC_PORT=33000 (docker default)
#   MT5_HEALTH_PORT=38002 (docker default)

# === Container (Optional) ===
# MT5_CONTAINER_NAME=mt5linux-test

# === Timeouts (Optional - in seconds) ===
# MT5_TIMEOUT_CONNECTION=300
# MT5_STARTUP_TIMEOUT=420
# MT5_STARTUP_HEALTH_TIMEOUT=30.0

# === gRPC Settings (Optional) ===
# MT5_GRPC_MAX_MESSAGE_SIZE=52428800
# MT5_GRPC_KEEPALIVE_TIME_MS=30000
# MT5_GRPC_KEEPALIVE_TIMEOUT_MS=10000

# === Retry Settings (Optional) ===
# MT5_RETRY_MAX_ATTEMPTS=3
# MT5_RETRY_MIN_INTERVAL=0.5
# MT5_RETRY_MAX_INTERVAL=5.0

# =============================================================================
# RESILIENCE SETTINGS (Optional)
# =============================================================================
# Enable for production stability. All default to false for backward compat.
#
# MT5_ENABLE_CIRCUIT_BREAKER=true   # Prevent cascading failures
# MT5_ENABLE_AUTO_RECONNECT=true    # Auto-reconnect with exponential backoff
# MT5_ENABLE_HEALTH_MONITOR=false   # Background health monitoring task
#
# Circuit Breaker tuning:
# MT5_CB_THRESHOLD=5                # Failures before opening circuit
# MT5_CB_RECOVERY=30.0              # Seconds before half-open state
# MT5_CB_HALF_OPEN_MAX=3            # Max requests in half-open state
