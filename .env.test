# Test environment configuration for mt5linux
# These values match mt5linux/docker-compose.yaml defaults
#
# This file contains TEST ENVIRONMENT settings (ports, container name)
# Credentials should be in .env file (MT5_LOGIN, MT5_PASSWORD, MT5_SERVER)

MT5_HOST=localhost
MT5_RPYC_PORT=38812
MT5_VNC_PORT=33000
MT5_HEALTH_PORT=38002
MT5_CONTAINER_NAME=mt5linux-unit
